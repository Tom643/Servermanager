android {  
    signingConfigs {
        release {
            storeFile file("/path/to/your/keystore/file.jks")
            storePassword System.getenv("p455w0rd")
            keyAlias System.getenv("awesome")
            keyPassword System.getenv("53cr3t")
        }
    }
}
